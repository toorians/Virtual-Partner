export default function ProjectName({title,name}) {
    return (
        <>
            <div className='projectName'>
                <p>{title}</p>
                <h1>{name}</h1>
            </div>

        </>
    )
}